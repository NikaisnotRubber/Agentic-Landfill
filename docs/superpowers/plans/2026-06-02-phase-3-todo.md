# Phase 3 執行 To-Do

> **分支：** `feat/phase-2-vm-identity-excel`  
> **最後更新：** 2026-06-02

## 工程底座

- [x] **REQ-ENG-001** — `.github/workflows/ci.yml`（`pnpm test` + `pnpm build`）
- [x] **REQ-ENG-002** — Vite 路由測試拆至 `tests/helpdeskViteRoutes.test.ts`（避免載入完整 `vite.config` 逾時）
- [x] **REQ-ENG-003** — `tests/fixtures/processed-payload-contract.json` + `phase3Engineering.test.ts`
- [x] 抽出 `server/helpdeskVitePlugin.ts`，`vite.config.ts` 精簡

## AD 品質

- [x] **REQ-AD-001** — `repairMojibakeText` + `normalizeAdEntry` 套用
- [x] **REQ-AD-002** — `resolveEffectiveAdAccount`：有 `ticket.ad.adAccount` 時不標 `missing-ad-account`

## 營運腳本（Phase 4 預覽）

- [x] `pnpm workflow:ddp` — fetch-and-enrich → export Excel 一鍵

## 驗收

- [x] `pnpm test` 全綠
- [x] 您確認後合併 `master`

## 後續（Phase 4 / Track B）

- SharePoint、排程強化
- DB 內容總表（0602）
