# Phase 2 執行 To-Do（目前有效清單）

> **最後更新**：2026-06-02  
> **開發分支**：`feat/phase-2-vm-identity-excel`（合併 `master` 前須使用者確認）  
> **分支說明**：[`2026-06-02-branch-workflow.md`](2026-06-02-branch-workflow.md)  
> **前提**：Phase 1 及更早里程碑**已確認完成**；本檔僅追蹤 **Phase 2A → 2B**。  
> **需求對照**：[`specs/2026-06-02-helpdesk-backlog.md`](../specs/2026-06-02-helpdesk-backlog.md)  
> **儀表板**：[`docs/PROGRESS.md`](../../PROGRESS.md)

---

## Phase 1 完成確認（2026-06-02）

下列能力**已在 repo 可運行**，與 `docs/superpowers/specs/` 2026-05-22～05-29 設計一致；**不需重開發**，僅文檔／細部補完。

| 能力 | 狀態 | 主要模組 |
|------|------|----------|
| Helpdesk 登入 / session | ✅ | `server/auth/*`, `pnpm auth:login` |
| Live / Sample + DDP 主旨過濾 | ✅ | `helpdeskFilters.ts` |
| Fetch + Enrich / Enrich Current | ✅ | `fetchAndEnrichTickets.ts`, `enrichAdRoute.ts` |
| Processed DDP View + abnormal flags | ✅ | `server/ddp/*`, `App.vue` |
| 新工單 tracker | ✅ | `newTicketTracker.ts` |
| NB/VM 描述解析 + Zentera VM（初版） | ✅ | `extractHostnamesFromDescription.ts`, `zentera/*` |
| First/Last Name（保留大小寫） | ✅ | `parseAdName.ts` |
| Manager sAMAccountName | ✅ | `resolveManagerSamAccount.ts` |
| Excel 匯出（三 sheet、樣式、連結） | ✅ | `server/excel/*`, Export Excel UI |

### Phase 1 殘留細部（不阻塞 Phase 2，記入 Phase 3+）

| 項目 | 說明 | 原 backlog ID |
|------|------|----------------|
| 中文 AD Name 亂碼 | 個案排查 | REQ-AD-001 |
| `missing-ad-account` 與 mail 並存 | 旗標邏輯釐清 | REQ-AD-002 |
| BU 僅 LDAP、非 Python HTTP | 已接受則標 wont-fix | REQ-AD-003 |
| CI / vite 路由測試逾時 | 工程項 | REQ-ENG-001/002 |
| SharePoint / 排程 / 平台 DB | 另階段 | Phase 4、Track B |

---

## 決策（Phase 2A）

- [x] **D1** — Excel 異常與 Processed flags **一致**
- [x] **D2** — VM 鏈：申請人直屬 **manager** → Zentera Role → Hostname

---

## Phase 2A — VM + 申請人身分 + 異常對齊 ✅（2026-06-02）

**目標：** Processed 與 Excel 共用同一套 VM／身分／異常規則。

### 2A.1 規格與決策

- [x] [`specs/2026-06-02-vm-lookup-and-identity.md`](../specs/2026-06-02-vm-lookup-and-identity.md)
- [x] 申請人帳號優先序 → **REQ-ID-001**

### 2A.2 VM 查詢鏈 → **REQ-VM-001**

- [x] `readApplicantManagerAccount` + `resolveVmHostname` 文件化
- [x] 描述 VM 優先、Zentera 多 VM 處理
- [x] `tests/zenteraVmLookup.test.ts`（申請人 ≠ manager 案例）

### 2A.3 申請人身分 → **REQ-ID-001**

- [x] `extractIdentityFromDescription` / `resolveIdentityFromDescriptionAndRequester`
- [x] `tests/phase2aIdentityAndAbnormal.test.ts`、`tests/ddpParsing.test.ts`

### 2A.4 異常規則 → **REQ-EXL-002**

- [x] `server/ddp/abnormalFlags.ts` + Excel 映射
- [x] `docs/helpdesk-workflow.md`

### 2A.5 驗收

- [x] `pnpm vitest run`（104 tests，排除偶發 vite 路由測試）
- [ ] **待您確認**：手動 Fetch + Enrich → Processed → Export Excel
- [x] `docs/PROGRESS.md` 已更新

---

## Phase 2B — Excel Role/Application + 匯出測試 ✅（2026-06-02）

**依賴：** Phase 2A 完成。

### 2B.1 規則與資料

- [x] Role：`Server_Profiles` 對照 VM；fallback `([A-Za-z]{2})\d+$`（notebook）
- [x] `server/zentera/resolveZenteraExportFields.ts` + `buildZenteraIndexes` 擴充
- [x] `ProcessedDdpRow` + Processed UI：`role`、`application`、`userRoles`

### 2B.2 匯出與測試

- [x] Excel 欄位 Role、Application、User Roles
- [x] NAS / Template / Location 等仍留空（**REQ-EXL-003** 刻意無來源）
- [x] `tests/fixtures/export-golden-columns.json` + `tests/phase2bZenteraExport.test.ts`

### 2B.3 驗收

- [x] `pnpm vitest run`（含 phase2b）
- [ ] **待您確認**：Export Excel 抽樣
- [x] `docs/PROGRESS.md` 已更新

---

## Phase 2 完成後才開工（不在本清單）

| 階段 | 內容 |
|------|------|
| Phase 3 | AD 亂碼、CI、payload 契約測試 |
| Phase 4 | SharePoint、排程、`pnpm workflow:ddp` |
| Track B | REQ-PLAT-001 DB 內容總表（[`to-do/0602.md`](../../../Mapping%20ADGroup%E3%80%81Zentera/docs/to-do/0602.md)） |

---

## 進度勾選慣例

1. 完成任務 → 本檔 `- [ ]` 改 `- [x]`
2. 關閉需求 → [`specs/2026-06-02-helpdesk-backlog.md`](../specs/2026-06-02-helpdesk-backlog.md) 對應 REQ 標 ✅
3. 迭代結束 → [`docs/PROGRESS.md`](../../PROGRESS.md) 迭代紀錄 + **最後更新**日期
